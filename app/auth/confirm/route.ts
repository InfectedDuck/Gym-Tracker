import{NextRequest,NextResponse}from"next/server";import{supabase}from"@/lib/supabase/server";import{safePath,withNext}from"@/app/auth/paths";export async function GET(request:NextRequest){const hash=request.nextUrl.searchParams.get("token_hash");const type=request.nextUrl.searchParams.get("type");const next=safePath(request.nextUrl.searchParams.get("next"));if(hash&&(type==="email"||type==="signup"||type==="recovery")){const client=await supabase();const{error}=await client.auth.verifyOtp({token_hash:hash,type});if(!error)return NextResponse.redirect(new URL(type==="recovery"?"/reset-password":next,request.url))}return NextResponse.redirect(new URL(withNext("/auth/invalid",next),request.url))}
