import{test,expect}from"@playwright/test";test("a modal keeps typed data on a padding click and returns focus to its trigger",async({page})=>{await page.goto("/exercises");const trigger=page.getByRole("button",{name:"Custom exercise",exact:true});await trigger.click();const dialog=page.getByRole("dialog");await expect(dialog).toBeVisible();await page.getByLabel("Exercise name").fill("Padding click test");await dialog.click({position:{x:6,y:6}});await expect(dialog).toBeVisible();await expect(page.getByLabel("Exercise name")).toHaveValue("Padding click test");await page.keyboard.press("Escape");await expect(dialog).toBeHidden();await expect(trigger).toBeFocused();await trigger.click();await expect(dialog).toBeVisible();await page.mouse.click(4,4);await expect(dialog).toBeHidden();await expect(trigger).toBeFocused()});test("the rest timer announces completion from one live region, moves focus, and round-trips pause and reload",async({page})=>{await page.goto("/");await expect(page.getByRole("heading",{name:"Today",exact:true})).toBeVisible();const status=page.getByRole("status",{name:"Rest timer status"});await expect(status).toBeAttached();await expect(status).toHaveText("");const region=await status.evaluateHandle(el=>el);const launch=page.getByRole("button",{name:"Open rest timer",exact:true});await launch.click();await expect(page.getByRole("button",{name:"Start timer",exact:true})).toBeFocused();await expect(page.getByRole("timer")).toHaveText("1:30");await page.evaluate(()=>window.dispatchEvent(new CustomEvent("forge-rest",{detail:1})));await expect(status).toHaveText("Rest complete");expect(await region.evaluate(el=>el.textContent)).toBe("Rest complete");await page.evaluate(()=>window.dispatchEvent(new CustomEvent("forge-rest",{detail:120})));await expect(status).toHaveText("");await page.getByRole("button",{name:"Pause timer",exact:true}).click();await expect(page.getByRole("button",{name:"Start timer",exact:true})).toBeVisible();await page.reload();await expect(page.getByRole("button",{name:"Start timer",exact:true})).toBeVisible();await expect(page.getByRole("timer")).toHaveText(/^(2:00|1:5[0-9])$/);await page.getByRole("button",{name:"Start timer",exact:true}).click();await expect(page.getByRole("button",{name:"Pause timer",exact:true})).toBeVisible();await page.reload();await expect(page.getByRole("button",{name:"Pause timer",exact:true})).toBeVisible();await page.getByRole("button",{name:"Reset timer",exact:true}).click();await expect(page.getByRole("timer")).toHaveText("2:00");await page.getByRole("button",{name:"Close timer",exact:true}).click();await expect(launch).toBeFocused()});