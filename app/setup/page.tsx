import{configured}from"@/lib/supabase/server";import{redirect}from"next/navigation";export const dynamic="force-dynamic";export default function Setup(){if(configured())redirect("/");return<main className="auth-page"><section className="auth-card setup-card"><div className="brand"><span className="brand-mark">F</span><span>Forge</span></div><p className="eyebrow">ONE-TIME WORKSPACE SETUP</p><h1>A home for your training.</h1><p>Connect your private database to enable accounts, workouts, and progression history.</p><ol><li>Create a development project in Supabase.</li><li>Copy <code>.env.example</code> to <code>.env.local</code> and fill in the project URL and publishable key.</li><li>Apply the two SQL files in <code>supabase/migrations</code> in filename order.</li><li>Configure email confirmation and recovery using <code>docs/SETUP.md</code>, then restart the application.</li></ol><p>Your workouts will be saved in PostgreSQL. No account or workout data is being stored on this setup screen.</p><a href="/" className="button primary">Check connection</a></section></main>}