import{test,expect}from"@playwright/test";test("writing your own exercise adds it straight to the workout",async({page})=>{await page.goto("/");await page.getByRole("button",{name:"New workout",exact:true}).click();await page.getByRole("button",{name:"Add exercise",exact:true}).click();await page.getByRole("button",{name:"Write your own exercise",exact:true}).click();await page.getByLabel("New exercise name",{exact:true}).fill("E2E freestyle move");await page.getByRole("button",{name:"Add it to my workout",exact:true}).click();await expect(page.getByText("Your exercise was added.")).toBeVisible();await expect(page.getByRole("link",{name:"E2E freestyle move",exact:true}).first()).toBeVisible()});