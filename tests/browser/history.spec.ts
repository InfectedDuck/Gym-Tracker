import{test,expect}from"@playwright/test";test("the history calendar opens a logged day and lists what was done",async({page})=>{await page.goto("/history");await expect(page.getByRole("heading",{name:"Calendar",exact:true})).toBeVisible();await expect(page.getByText("Sessions logged",{exact:true})).toBeVisible();const trained=page.getByRole("button",{name:/session/}).first();await expect(trained).toBeVisible();await trained.click();await expect(page.getByRole("link",{name:"Open"}).first()).toBeVisible();await expect(page.getByRole("heading",{name:"Recent sessions",exact:true})).toBeVisible();const rest=page.getByRole("button",{name:/rest day/}).first();await rest.click();await expect(page.getByRole("heading",{name:"Nothing logged that day",exact:true})).toBeVisible()});test("progress reports weekly sets, muscle balance and movement trends",async({page})=>{await page.goto("/progress");await expect(page.getByRole("heading",{name:"Progress",exact:true})).toBeVisible();await expect(page.getByRole("img",{name:/Weekly training sets/})).toBeVisible();await expect(page.getByRole("heading",{name:"Training activity",exact:true})).toBeVisible();await expect(page.getByRole("heading",{name:"Movements",exact:true})).toBeVisible();await expect(page.getByRole("columnheader",{name:"Est. 1RM",exact:true})).toBeVisible()});test("exercise history switches months year and all with trend",async({page})=>{await page.goto("/exercises/10000000-0000-4000-8000-000000000001");await expect(page.getByRole("heading",{name:"Barbell bench press",exact:true})).toBeVisible();await expect(page.getByText("Trend",{exact:true})).toBeVisible();await expect(page.getByRole("img",{name:/weight progression/})).toBeVisible();for(const name of["6 months","1 year","All"]){await page.getByRole("button",{name,exact:true}).click();await expect(page.getByRole("img",{name:/progression/})).toBeVisible()}await page.getByRole("button",{name:"3 months",exact:true}).click();await expect(page.getByLabel("Find movement")).toHaveCount(0);await page.goto("/progress");await page.getByLabel("Find movement").fill("bench");await expect(page.getByRole("link",{name:"Barbell bench press",exact:true}).first()).toBeVisible()});
