import{bootstrap,requireIdentity}from"@/lib/data";import{AppProvider}from"@/components/provider";import{Shell}from"@/components/shell";export const dynamic="force-dynamic";export default async function DashboardLayout({children}:{children:React.ReactNode}){await requireIdentity();const initial=await bootstrap();return<AppProvider initial={initial}><Shell>{children}</Shell></AppProvider>}