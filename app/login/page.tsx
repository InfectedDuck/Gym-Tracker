import{AuthForm}from"@/components/auth-form";import{safePath}from"@/app/auth/paths";import{configured}from"@/lib/supabase/server";import{redirect}from"next/navigation";export const dynamic="force-dynamic";export default async function Page(props:PageProps<"/login">){if(!configured())redirect("/setup");const{next}=await props.searchParams;return<AuthForm next={safePath(next)}/>}
