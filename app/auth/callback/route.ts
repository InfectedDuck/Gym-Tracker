import{NextRequest,NextResponse}from"next/server";import{supabase}from"@/lib/supabase/server";import{safePath,withNext}from"@/app/auth/paths";export async function GET(request:NextRequest){const code=request.nextUrl.searchParams.get("code");const next=safePath(request.nextUrl.searchParams.get("next"));if(code){const client=await supabase();const{error}=await client.auth.exchangeCodeForSession(code);if(!error)return NextResponse.redirect(new URL(next,request.url))}return NextResponse.redirect(new URL(withNext("/auth/invalid",next),request.url))}
